using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Character : MonoBehaviour
{
    /// <summary>
    /// Percent/damage for knockback scaling.
    /// </summary>
    private float Percent { get; set; } = 0f;

    /// <summary>
    /// Charge meter for attacks acquired by wavedashing.
    /// </summary>
    private float Charge { get; set; } = 0f;

    // Start is called before the first frame update.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
